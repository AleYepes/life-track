# ActivityWatch Title Extender

A lightweight Chromium extension that enriches browser tab titles with metadata (URLs, audio states, YouTube channel names/IDs, Gmail senders) so that OS window-trackers like ActivityWatch can capture browser context indirectly.